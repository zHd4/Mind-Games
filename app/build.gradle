plugins {
    id 'application'
    id 'checkstyle'
}

group = 'app.mindgames'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

application {
    mainClass = 'app.mindgames.App'
}

compileJava {
    options.release = 20
}

run {
    standardInput = System.in
}

dependencies {
    testImplementation platform('org.junit:junit-bom:5.9.1')
    testImplementation 'org.junit.jupiter:junit-jupiter'
}

test {
    useJUnitPlatform()
}